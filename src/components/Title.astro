---
interface Props {
  subtitle?: string
}

const { subtitle } = Astro.props
---

<div class="flex flex-col gap-2.5 w-fit">
  <h2 class="text-[var(--color-secondary)] text-lg font-bold text-center"><slot /></h2>
  { subtitle !== undefined && <span class="text-center">{subtitle}</span> }
</div>
